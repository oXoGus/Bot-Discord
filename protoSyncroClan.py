import sqlite3

# conection to the db 

# add i column 


while 1:
    #request clan 
    # request player data
    # i = i + 1
    try : 
        #try search table with the tag of the clan
        # for player in clanMember    
        # if player already in the db 
        # update player json and i
        # else send join message + insert new player  
        # cursor.execute("SELECT FROM clanMember_{clanTag} WHERE I = ?", (i-1,))
        # for player in cursor.fetchall :
        # send leave message + delet player from the db
        pass    
    except : 
        #  for player in clanMember 
        # insert player and i 
        pass